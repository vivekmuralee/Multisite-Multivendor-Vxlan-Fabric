[defaults]
inventory       = inventory/hosts.yml
remote_user     = cumulus
host_key_checking = False
retry_files_enabled = False
stdout_callback = ansible.builtin.default
result_format   = yaml
vault_password_file = ~/.ansible/cumulus_vault_pass

[privilege_escalation]
become          = True
become_method   = sudo
become_user     = root
